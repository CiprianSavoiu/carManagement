{
  "name": "car-manager-CipSa",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://github.com/CiprianSavoiu/carManagement",
  "scripts": {
    "start": "react-scripts starts",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
